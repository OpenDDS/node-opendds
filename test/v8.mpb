project {
  lit_libs += v8

  specific(prop:microsoft) {
    includes += $(V8_ROOT)/include
    Debug::libpaths += $(V8_ROOT)/build/Debug/lib
    Release::libpaths += $(V8_ROOT)/build/Release/lib
  }
  // Linux uses system packages for v8, so /usr/include is sufficient.
}
